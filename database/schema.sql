CREATE DATABASE IF NOT EXISTS agencia_tours;
USE agencia_tours;

-- El modelo principal se gestiona con Prisma en backend/prisma/schema.prisma.
-- Ejecuta: cd backend && npm run prisma:migrate && npm run seed
